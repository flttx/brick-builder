# CI

建议 CI：Install → Typecheck → Lint → Unit → Engine Integration → Build → Asset Validation → Backend Integration → E2E Smoke。

并行 Jobs：quality、engine-tests、backend-tests、asset-validation、web-build、e2e。

PR 保持快且稳定；5000 Brick、复杂离线、多设备冲突、完整 Migration Matrix 放 Nightly。
