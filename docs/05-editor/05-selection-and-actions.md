# Selection 与操作

V1 先支持 Single Selection。

选中后支持：Rotate、Duplicate、Delete、Color、Precision Connect、Focus。

Duplicate 保留 partId/colorId，但不复制旧 Connection，生成新 Brick ID 后进入 Placement Session。

Multi-select、Lasso、Assembly 操作延后。
