# Editor Experience

Editor 应保持 Canvas 为主，工具为辅。桌面和移动端都避免 CAD 式密集永久工具栏。

主要入口：Bucket、Parts、Color、Rotate、Undo/Redo。选中 Brick 后显示 Context Actions：Rotate、Duplicate、Precision Connect、Delete。

Canvas 不因打开面板而重建。
