# 设备适配与质量降级

质量档：Auto / High / Balanced / Performance。

降级顺序优先关闭：Bucket Physics → AO/Contact Shadow → High Shadow → High DPR → High LOD → Decorative Effects。

绝不能因为设备性能低而降低 Snap、Collision、Connector、Save 的正确性。
